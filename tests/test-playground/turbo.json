{
	"$schema": "../../node_modules/turbo/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["app/**", "src/**", "public/**", "*.config.*", "package.json"],
			"outputs": ["build/**"]
		}
	}
}
