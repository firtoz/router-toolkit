{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"inputs": ["app/**", "src/**", "public/**", "*.config.*", "package.json"],
			"outputs": ["build/**"]
		}
	}
}
